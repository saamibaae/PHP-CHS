This project was previously a Flask app. To create a separate GitHub repository that contains only the PHP version, follow the steps in `README.md` or run `reinit_repo.ps1`.

Be sure to backup any files you need before removing the `.git` directory.
